# Application role: master (full studio) | slave (execution-only agent)
VITE_APP_ROLE=master

# Dump every captured GraphQL request during Facebook crawls to debug_dumps/crawl/{post_id}/
FACEBOOK_CRAWL_DUMP_REQUESTS=false
